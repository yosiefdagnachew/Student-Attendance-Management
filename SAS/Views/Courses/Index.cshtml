@model IEnumerable<SAS.Models.Course>

@{
    ViewData["Title"] = "Index";
}

<div class="content-header" style="padding-left:250px; color:black;padding-bottom: 40px;padding-top: 10px;">
    <a asp-action="Create" asp-controller="Courses" class="btn btn-primary" style="float:right;"> Add New  </a>
</div>

<div class="mainbody" style="padding-left:250px;">
    <table id="example" class="display" style="width:100%">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Course_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course_code)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course_credit)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course_descrption)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Course_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course_code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course_credit)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course_descrption)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Course_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Course_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Course_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</div>